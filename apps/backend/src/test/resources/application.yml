spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true
sdbrewpi:
  hardware:
    enabled: false
  simulation:
    enabled: false
  mqtt:
    enabled: false
    freshness-seconds: 90
  web:
    allowed-origins: http://localhost:5173,http://127.0.0.1:5173
